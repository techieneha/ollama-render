services:
  - type: web
    name: ollama-api
    runtime: docker
    region: oregon
    plan: starter
    dockerfilePath: ./Dockerfile
    envVars:
      - key: OLLAMA_MODELS
        value: mistral
      - key: PORT
        value: 11434
    autoDeploy: true
