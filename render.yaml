services:
  - type: web
    name: ollama-api
    runtime: docker
    plan: starter
    region: oregon
    dockerfilePath: ./Dockerfile
    envVars:
      - key: OLLAMA_MODELS
        value: mistral  # Or whatever model you're using
      - key: PORT
        value: 11434
    autoDeploy: true
